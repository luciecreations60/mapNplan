# Future public API configuration. Never commit secret keys.
VITE_APP_ENV=development

# Supabase (values are public/publishable — safe to expose in a client app,
# access is enforced server-side via Row Level Security)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
